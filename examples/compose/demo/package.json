{
  "name": "demo",
  "version": "1.0.0",
  "description": "escriba demo application",
  "scripts": {
    "build": "rimraf .parcel-cache dist && parcel build src/index.html",
    "start": "rimraf .parcel-cache dist && parcel src/index.html",
    "app": "npm run build && http-server dist"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@mycloudlab/escriba-browser": "^0.8.14"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "parcel": "^2.8.1",
    "rimraf": "^3.0.2"
  }
}
